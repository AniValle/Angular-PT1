import { NotNumerosDirective } from './not-numeros.directive';

describe('NotNumerosDirective', () => {
  it('should create an instance', () => {
    const directive = new NotNumerosDirective();
    expect(directive).toBeTruthy();
  });
});
