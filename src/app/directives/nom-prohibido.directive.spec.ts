import { NomProhibidoDirective } from './nom-prohibido.directive';

describe('NomProhibidoDirective', () => {
  it('should create an instance', () => {
    const directive = new NomProhibidoDirective();
    expect(directive).toBeTruthy();
  });
});
